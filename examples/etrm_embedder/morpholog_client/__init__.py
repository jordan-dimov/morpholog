"""A typed Morpholog client for programme `trade_lifecycle`.

GENERATED by `morpholog generate python-client` - do not edit;
regenerate when the programme changes. The model hash in
`__init__.py` names the rules this client was built against.
"""

import sys

if sys.version_info < (3, 10):
    raise RuntimeError(
        f"morpholog_client requires Python 3.10+ "
        f"(running {sys.version_info.major}.{sys.version_info.minor}); "
        f"the generated code holds a conservative floor on purpose"
    )

PROGRAM = "trade_lifecycle"
MODEL_HASH = "sha256:99e750741460f0f14be19d791636476b2d8c1b1eeb87a2a01a4dccd634a5cee2"
MORPHOLOG_VERSION = "0.0.1"
PYTHON_FLOOR = (3, 10)

from . import envelopes, models, values  # noqa: E402
from .adapter import Morpholog, MorphologError  # noqa: E402

__all__ = [
    "PROGRAM",
    "MODEL_HASH",
    "MORPHOLOG_VERSION",
    "PYTHON_FLOOR",
    "Morpholog",
    "MorphologError",
    "envelopes",
    "models",
    "values",
]
