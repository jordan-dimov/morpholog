# Morpholog: Roadmap

Status: operational. This document is what the project is *doing next*. It is forward-looking and lower-stakes than [`scope-and-ambition.md`](scope-and-ambition.md) (which fixes what Morpholog is *for*) and [`runtime-semantics.md`](runtime-semantics.md) (which fixes what the runtime *means*). When this file and design-history disagree, design-history is the historical record; this file is intent.

The "forced by a worked example" discipline binds **kernel and IR primitives**: a new `Prop` / `ValueExpr` / `Stmt` / `Value` variant earns its place only when an example needs it, never speculatively. That is where unconstrained growth is dangerous, and the bar stays high. It is *not* a brake on everything else - tooling, new worked examples, performance work, legibility surfaces, and product completeness move as soon as the direction is clear, not only when an example forces them. The bar for *adding to the language* is deliberately higher than the bar for *building around it*. This is intent, not a wish list.

## Status today

The kernel, PG adapter, CLI, polling outbox worker, and the worked examples are all in good shape. The parser arc is closed and the examples are `.morph`-canonical, round-trip-coupled to the formatter. The language arc's first rungs have landed (`define`, claim disciplines, the lint tier, source-located diagnostics). The embedder contract is pinned end to end - codecs, schemas and the manifest, the outcome envelopes, batch import, the outbox lease protocol, and the generated Python client that makes the whole contract travel in the binary ([`embedder-integration.md`](embedder-integration.md)); a real external embedder drives a governed trade lifecycle through it, and its friction is a standing source of forcing pressure. The value vocabulary covers time and units where examples forced them; local time and unit conversions enter as claims, never as runtime lookups.

That is all this section says, by rule: **shipped work leaves this file**. What Morpholog does is the [README](../README.md)'s job; what forced each piece and why is [`design-history.md`](design-history.md)'s; this file holds only what comes next.

## The arcs

The forward picture is a set of arcs over one IR - the language arc, the kernel arc, and the verification arc - each a different reading of the same semantic truth the kernel already evaluates and renders. None changes the constitution; each deepens what the existing thesis delivers. The forcing discipline holds throughout: kernel and surface primitives still arrive with the example that forces them; the arcs name the direction, not a backlog.

**The language arc - doctrine into declarations.** `define` (landed) named the conditions; **claim disciplines** (landed) put the append-only / retractable-pointer / lineage trichotomy and structural uniqueness on the predicate declarations, lowered to generated invariants or enforced statically. The metadata makes the gate-vs-invariant doctrine machine-checkable; the lint itself rides with the `--strict` layer. The remaining rung is **parametric patterns** (predicate-parameterised rule templates - restatement, standing, the cross-example idioms as artifacts), awaiting a worked example that genuinely reuses a pattern across domains rather than echoing its shape.

**The kernel arc - the interpreter becomes the spec.** In dependency order: invariant pruning by read/write-set intersection (the walkers exist; days of work when reached); a node-identified IR with a shared fold (ends the per-pass walker duplication; the source-location layer shipped separately - the surface-side `SourceMap` already points validation errors, lints, and runtime rejections at `.morph` declarations and statements, and the shared fold would deepen that to sub-expression granularity); a layered candidate state (base + delta overlay - the base is the pre-state, which dissolves the blocker recorded against the delta-aware rebuild); then the deep one, a **spike compiling the relational core of invariant checking to SQL** executed inside the same SERIALIZABLE transaction. That single mechanism would subsume argument-sensitive scoped loading (the compiled WHERE clause is the argument-sensitive footprint), narrow the SSI read footprint the contend bench measures, and stop commit latency scaling with in-scope state - with the sync kernel retained as the executable specification and a differential-test harness over the worked examples as the correctness argument. One spec requirement recorded up front: the compiled residual checks must be *case-bound* (inconsistency-tolerant, per [`prior-art.md`](prior-art.md)) - rule-version adoption and exception claims mean commit-time checking cannot assume a globally clean history, only that the new write introduces no fresh violation. Spike and measure against the bench before committing to it.

**The verification arc - from explaining to proving.** `explain` answers "why not"; `inspect guarantees` and `controls` answer "what is forbidden and what must be true first", and `controls` now also draws the gate-protection map - which gate front-loads which invariant, a syntactic correspondence through shared positive predicates, never a proof of entailment (see [`design-history.md`](design-history.md)); `inspect coverage` (the arc's opener) answers "which rules have ever actually done work" over the replayed audit log, and with the rejection log landed, "which rules have demonstrably refused" via the `constrained` verdict. The next tiers ask what the tooling still cannot see: **static vacuity** (an antecedent that cannot bind in any state the ledger can hold - the offline-oracle tier; note the shipped unsupplied-antecedent lint is *not* this, it is a declared-supplier authoring smell that says nothing about persisted or historical state - a true vacuity proof needs an enforced closed-world lifecycle or history context); and, furthest out, **bounded checking offline** (invariant mutual satisfiability, dead transformations, unreachable-without-claim questions) - an explicit revisit of the no-model-checker non-goal, never in the runtime, under the Cedar-style decidable-fragment discipline [`prior-art.md`](prior-art.md) already endorses. The same machinery is the road to bounded why-not (abduction): the repair-plan tier of the explanation engine, and the engine of the propose-refuse-repair loop the README promises for untrusted proposers.

The verification arc's furthest reading turns the evaluator **backward**: instead of gating a proposal or proving a property, it scores a *hypothesis against history*. Its first rung has landed - `evaluate` loads a candidate programme that is not deployed, replays the log, and reports per candidate invariant which already-admitted commits it would have refused (a fresh violation: the commit's result violates an invariant the prior state satisfied). It is a sibling of `coverage`, not a reuse: a committed rule held on every transition, so coverage asks "did its antecedent fire"; a candidate was never enforced, so the signal is exactly where history *violates* it. No kernel primitive - the runtime is now a discovery oracle as well as a gate. That is the substrate under a "discover, prove, govern, explain" loop: a proposer offers candidate controls over a corpus, Morpholog scores them deterministically, and the survivors a human accepts become governed, enforced, audit-grade invariants with provable lineage - the standing primitive already models the discovered -> provisional -> accepted -> enforced lifecycle, and the Merkle audit gives each accepted rule its provenance. The proposer, the search that recombines candidates, and the population that holds them stay **outside** the kernel; Morpholog is the deterministic, explainable scorer, which is the whole point where a black-box score is inadmissible. The overfitting guard has landed (`evaluate --train-until`: one continuous replay, per-slice attribution, offline packs split identically); what remains on this rung: the labels (known-bad / known-clean / corrected-later) and the multi-objective weighting that turn the counterfactual into a judged score stay in the proposer's harness, never the kernel - the runtime cross-examines, the harness decides what a catch is worth; definition fitness; and a transition-relational semantics for `pre(...)` candidates (v1 scores state invariants only). The value language discovery pulls forward (ratios, windowed aggregates, conditionals as derived reads) arrives under the usual discipline - each operator with the concrete discovered rule over real data that forces it, never a speculative statistics algebra; discovery is a forcing function precisely because the rule it surfaces is a worked example, not a licence to grow the value language ahead of one.

## Imminent

The prior art and influences behind the directions below live in [`prior-art.md`](prior-art.md). That file is possibility pressure; this one is sequencing.

- **The remaining lint occupants.** The lint tier exists (hints on stderr, `--strict` promotes; the gate-vs-invariant and unsupplied-antecedent lints opened it). Still to add: unused predicate / intent declarations, `sum(x | body)` where `x` doesn't appear in `body`, unused transformation parameters, fuzzy "did you mean `MayApprove`?" suggestions on an `Undeclared` reference.

- **Remaining legibility surfaces.** The explanation-engine doctrine stands - deterministic template rendering over the IR and trace, never natural-language generation - and its shipped tiers (`explain`, `inspect guarantees`, `inspect controls`, source-located diagnostics) are recorded in [`design-history.md`](design-history.md). Still to build: `inspect transformation --graph` (the body as a pre/post dependency DAG); subject-flow profiles (clusters where the same `Subject`-kind position recurs). The deeper control matrix - pairing each gate to the invariant it front-loads and naming the failure mode each guards against - shipped as the `front_loads` cross-link in `inspect controls`. All derive statically from the parsed programme; none adds IR primitives. Full multi-step path-finding is bounded model checking, deferred; predicate-kind annotations were considered and deferred - the classification is largely inferable from structure.

## Performance, when forced

The bench measures the curves that would force work here; the numbers below are the current readings (full detail in `crates/morpholog-bench/README.md`). Nothing here is forced today.

- **Single-propose scaling is linear, and the kernel is not the bottleneck.** ~1.6s per commit at 100K in-scope claims and ~1.5s for a 100K-transition replay are dominated by load + PG IO, not kernel work. Both are intrinsic to re-reading and re-indexing the full in-scope claim set each transition; the cure is materialisation, not micro-optimisation.
- **Retraction-heavy replay stays linear.** The `as-of --retract-fraction K` axis confirms it: a 50%-retract log replays in the same linear time as an asserts-only one (the `ReplaySet`'s retract is amortised `O(1)`). This was the measurement to take before sizing any snapshot/lattice work - and it says that work is **not forced**. The likely shape when it is: a state-snapshot table materialising the live claim set every K transitions, plus a delta-replay path resuming from the nearest snapshot (distinct from the audit Merkle *checkpoint* that shipped - that commits to the log's integrity; this would cache the claim set for replay speed). That is plain-PostgreSQL application logic - state-at-T is a stateful fold over assert/retract, not a time-bucket aggregate, so *not* something a TimescaleDB continuous aggregate could express. The discovery scoring loop - replaying many candidate rules over the same log - is the plausible first workload to force it, where today the asserts-only and retract-heavy curves say it is not.
- **Concurrent proposals on a shared predicate footprint anti-scale, and value-level partitioning does not fix it.** The `contend` scenario shows the SERIALIZABLE 40001 retry rate climbing roughly linearly with worker count while throughput falls: posts that read a predicate and write into it serialise under SSI. Crucially, the `--periods` sweep shows that sharding the writes by value (a private period per worker) leaves the retry rate flat - because `load_state` reads the whole predicate, not a value sub-range, so the read/write footprints still overlap. The `--disjoint` sweep confirms the positive half: giving each worker its own *predicate* roughly quarters the wall-clock and more than halves the retry rate. So the lever is disjoint predicate footprints (already exploited by predicate-scoped loading) or batching, not value-level sharding of one aggregate. The residual contention even when fully predicate-disjoint is consistent with PostgreSQL's predicate-lock granularity (the adjacent `Bench_*` keys likely share an index page) rather than logical overlap; confirming the mechanism would need a physical-layout control. This is also the measured 40001 rate the substrate question below requires.
- **Throughput levers, when concurrency on a hot predicate is forced.** The unit of concurrency is the read footprint the runtime loads, not the business value - so the levers, in increasing depth, are: *batch* the contending writes into one governed transition (one read-set pass for many entries, the natural shape for import / auction / settlement files); model *bounded aggregate claims* (`AccountBalance`, `EntryBalance`) so a transition reads a few aggregates instead of every base line (O(touched aggregates), not O(all lines)); *argument-sensitive scoped loading* - narrowing `load_state` to the predicate slice a transition needs (`JournalLine where period = p7`) rather than the whole predicate, which requires static read-set extraction with runtime parameter substitution, a real feature rather than a DB index; and *key-sliced write routing* - serializing same-footprint writes into one queue before they reach PostgreSQL eliminates hot-key 40001 at the gateway (the deterministic-concurrency literature's move, [`prior-art.md`](prior-art.md)), but needs a resident process, so it arrives with a `morpholog serve` if one is ever forced; until then it is embedder-side guidance. None is forced until a real workload needs the throughput; the bench's `contend` axis is where that pressure would first show.
- **TimescaleDB for the audit log - candidate substrate enhancement, when real scale forces it.** `morpholog.audit` is append-only and time-ordered, a natural hypertable on `committed_at`. Two wins would be real at multi-gigabyte scale: chunk exclusion for as-of window queries, and columnar compression of cold chunks (the repetitive-key JSONB payloads compress well, and compressed-chunks-are-read-only suits an immutable ledger - the strongest reason to reach for it, since the target domains produce GB-scale audit lakes). Hard constraints, because this touches the correctness substrate: single-node only (distributed hypertables drop cross-node `SERIALIZABLE`, non-negotiable, and are deprecated upstream regardless); the `transition_id` primary key must become composite with the partition column, so the as-of lookup path needs re-checking; and SSI behaviour across chunks (predicate-lock granularity, 40001 rate) must be *measured* on the partitioned shape, not assumed. It does not replace the state-snapshot work above. Adopting it would be an explicit revisit of the locked PG-only substrate floor. Timescale's analytics primitives stay on the BI side, outside governed reads. Revisit only when the bench surfaces a real storage or replay problem on a real workload - not before.
- **The subprocess CLI tax is low; a long-lived propose-worker is not forced.** `scripts/embedder_latency.sh` times one `morpholog propose` end-to-end at single-digit milliseconds (~3ms spawn + parse + validate, plus ~6ms connect + propose + commit against a small ledger) - on the order of 100 governed transitions per second, single-threaded. That is comfortable for the lifecycle events the governed core exists for (capture, confirm, settle, approve) and for admin / audit flows. A long-lived worker (load + validate once, warm pool, HTTP or socket) earns its place only when a genuinely high-frequency path needs it - and the doctrine keeps high-frequency analytical paths out of the governed core regardless. The propose component grows with in-scope state as the `write` scenario measures; the spawn / parse / validate floor does not. The one path where the spawn floor did bite is discovery scoring (a search is candidates x cases, so one `evaluate` launch per case is millions of spawns) - and it was answered read-side by `evaluate --packs <dir>` batching many cases into one process, not by a resident worker, which keeps that floor a non-issue without one.

## Operational completeness

The hardening real deployment needs. Not language growth and not gated on a worked example - these move as Morpholog approaches its first production deployment. Listed, not frozen.

- **Worker supervisor.** The polling outbox worker exists and ships a `StdoutDeliverer`. Missing: a supervisor running multiple workers under restart-with-intensity, with crash isolation between deliverers.
- **Per-target circuit breakers.** A delivery target that misbehaves should be ring-fenced (back off, alert, eventually quarantine), not continue eating the worker's loop indefinitely.
- **HTTP-aware deliverer.** Once a worked example actually sends an outbox intent over the wire, an `HttpDeliverer` with the right retry/idempotency semantics.
- **Parser-side input-depth guard.** `Program::validate` already rejects expression and `for`-statement nesting past a fixed depth, so over-deep IR cannot exhaust the stack in `propose` - the teeth behind "validate untrusted IR before proposing it". The `.morph` parser has no equivalent guard: a pathologically nested source file could overflow the recursive-descent parser before it produces any IR. Deferred because in v0 you author your own `.morph` files; forced once `parse` ingests source from an untrusted origin.
- **Least-privilege provisioning.** The honest answer to "can't someone bypass the gate with raw SQL?" is privilege separation - only the runtime's role writes the `morpholog` schema - plus `verify` making any out-of-band write tamper-evident. Both hold today, but the privilege half is hand-configured: `morpholog init` creates the schema and leaves grants to the operator. A `--least-privilege` mode that mints a dedicated writer role, revokes public DML on the governed tables, and prints the read-only grant a projector needs would make "the governed path is the only door" true on a fresh database by default rather than by convention - turning deployment guidance into a provisioned floor. The pitch leans on this property; the tooling should ship it.
- **`morpholog serve` - the daemon escape from the subprocess floor.** Every governed write is a `morpholog propose` subprocess today: ~3ms spawn over a ~6ms commit (`scripts/embedder_latency.sh`), comfortable at lifecycle cadence, and `propose --batch` already amortises the spawn across an import. The escape when a genuinely high-frequency *single-write* path is forced is a resident process - load and validate the programme once, hold a warm pool, take transitions over a socket or HTTP, and answer with the *same* JSON envelopes the CLI pins. That keeps the one-contract, process-isolation, and polyglot properties intact, which is precisely why the throughput answer here is a daemon rather than a language-specific in-process FFI (PyO3 / napi). Deferred until a real workload needs it; the bench's `contend` axis and the latency script are where that pressure first shows. The key-sliced write router under "Performance, when forced" is a feature *of* this process when it lands.
- **The embedder surface grows by forcing only.** The pinned contract and the generated Python client live in [`embedder-integration.md`](embedder-integration.md); what remains is whatever the next real embedder forces. Already visible on that horizon: a TypeScript client and an OpenAPI surface (each waits for the embedder that needs it), and the generated client's kind floor - `Duration` models still await an embedder, while `Collection` arguments now have a named forcer (the set-valued-proposal / batch-completeness example under "Language affordances").
- **Tamper-evident audit: the proofs still to come.** The Merkle history-tree foundation, the portable evidence pack, signed tree heads with keys as first-class ledger claims, and the **Certificate-Transparency proof tier** have all shipped (`checkpoint` / `verify`, `evidence export` / `evidence verify`, `keygen`): a complete checkpointed prefix, a **window** between two checkpoints carrying logarithmic consistency and per-row inclusion proofs, or a **selective pack** disclosing only chosen transitions - each disclosed row proven included at its position, undisclosed rows absent entirely - verified offline with no database access; a full-prefix pack judges signing-key authority from its own carried rows (an `AuditSigningKey` claim resolved as of the prefix), while window and selective packs check checkpoint signatures cryptographically only. What remains: the contiguous-range multiproof (a size optimisation over the shipped per-row inclusion form, possibly forced sooner by large windows); and **subject completeness** (inclusion and consistency cannot prove a pack holds *all* of one subject's history - a selective pack proves what it shows, never that the selection is complete; that needs a subject-indexed commitment, or a deliberately narrower claim). (`evaluate --pack` - scoring candidates against a verifiable pack with no database - has shipped.)

## Language affordances awaiting a worked example

Each lands when an example actually demands the shape. None are pre-decided.

- **Conway's Game of Life - whole-state proposal stripped of business noise.** Set-valued proposal itself shipped: collection arguments now flow across the typed contract (the JSON-Schema contract, the generated client, the named-argument codec), with element kinds inferred from `for` / `forall` usage, forced by the **margin-call-run example** (example 14) and its completeness lesson - the proposed batch must *contain* everything the rule requires, so a *missing* required line is refused, not merely an extra one (totality, not just exclusion). The deferred miniature that remains is Conway's Game of Life: not a simulator, but the smallest fully-determined world where a proposer submits a whole successor snapshot and Morpholog admits it only if it is exactly the lawful successor - the same shape, stripped of business noise. A forcing toy for whole-state proposal, if it earns its place.
- **Higher-order authority / predicate-pattern matching.** *One* authority claim governing a *family* of transformations, instead of one per kind. The declared predicate vocabulary now provides a metadata home; the shape itself still needs a worked example.
- **Cryptographically authenticated actors (authorization, then authentication).** The `--actor` is self-reported text today: the embedder's auth layer authenticates the user and Morpholog records who the app *says* acted, then gates authority on it. That is right for a single trusted application, and the audit log makes any *database* tampering evident - but it cannot prove the application layer did not lie to the chokepoint about who authorized an action. In a low-trust, multi-writer topology - counterparties posting to a shared ledger, where neither trusts the other's API gateway - a compromised or rogue layer can forge the actor. Crossing from authorization into *authentication* - a transition carrying an Ed25519 signature over its payload by a key whose authority is an admitted `AuditSigningKey` claim, resolved as-of the same way - reuses the keys-as-claims rails the signed-checkpoint work already built. It is a deliberate boundary shift (heavy for a single-app deployment, load-bearing for zero-trust multi-party), so it waits for the worked example where a forged actor is the actual vulnerability: a multi-writer settlement or inter-desk posting whose proposers genuinely do not trust each other. `system_actor()` is the placeholder until that lineage model lands.
- **Parametric patterns.** Predicate-parameterised rule templates, instantiated explicitly and expanded at load - the restatement triple, the standing pattern, the cross-example idioms shipped as artifacts instead of READMEs. The rung after claim disciplines on the language arc; it waits for the disciplines to prove the lowering-plus-metadata shape, and for a worked example that genuinely reuses a pattern across domains rather than echoing its shape.
- **Effective time as a separate axis.** As-of gives knowledge time. Effective time - the day a contract becomes binding, the period a posting reflects - is expressible as ordinary claims; combining the two gives bitemporal addressability without `valid_from`/`valid_to` schema columns.
- **Validity windows + repair transitions + exception claims.** First-class typed claims that mark an admitted assertion as in-repair, contested, or quarantined, with audit standing. No bypass flags ever.
- **Materialised derived claims as governed state.** The operational read-cache form has shipped (`morpholog refresh derived` -> `morpholog_read`): a discardable projection whose freshness is operational metadata (model hash, refresh time, and the latest audit transition visible in the refresh snapshot - a marker, not a lossless high-water). What remains deferred is the *governed* form - materialised snapshots whose invalidation is modelled as ordinary claims rather than cache machinery - and the cheaper-refresh tiers the read cache will force when scale demands them (partitioned / incremental / affected-key refresh, then a streaming `enumerate_derived`). The current refresh is exact and full; its scaling boundary is documented in [`design-history.md`](design-history.md). One small operational legibility gap alongside it: a derived view of a stale or mismatched cache reads as empty, which a dashboard cannot tell apart from "no rows" - a companion metadata view (model hash, refreshed-at, latest visible transition, per-predicate row count) emitted beside the data views would let a reader distinguish empty-because-stale from empty-because-nothing.
- **Migrations framework.** A small one. The schema is hand-written today; with multiple deployments and an evolving claim vocabulary, a migrations story will be forced. The shape should be claim-shaped (migration as transformation, schema version as admitted claim).
- **Compact two-sided bounds (surface sugar).** Two of the three forms have shipped, each landing when a programme read meaningfully better for it: `abs(x)` (the cadence net-position cap - one sum, one comparison) and chained comparison `a <= x <= b` (the borrowing-base advance-rate band, lowering to the same `and` of pairwise comparisons - see the surface-to-IR table). What remains is unary minus (`-x` for `0 - x`): ergonomic, but a deliberate step toward surface negation - distinct from the still-barred signed *literals* - and `abs` made it non-essential, so it waits for a programme that genuinely reads better with it. **Held back, not sugar:** an invariant referencing a *derived* predicate (`NetPosition(book, net) and PositionLimit(book, limit) implies abs(net) <= limit`) is the most elegant form, but it makes read-side derived claims commit-relevant - derivation would run over candidate state inside `propose`, crossing the read-side/commit-side boundary kept on purpose. That is its own design question and PR, not part of the sugar.
- **Civil-date arithmetic, and the calendar ergonomics around it.** Time arithmetic now exists where the laytime example forced it: exact instants (`Timestamp`) and exact spans (`Duration`), with instants shifting by spans, differencing into spans, and spans summing and capping. The finance critique - "you cannot govern lending or accruals without business-day calendars and date math" - lands on a real *ergonomic* gap, not an architectural one, and the answer is two distinct tracks. (1) **The kernel primitive:** arithmetic on *civil dates* - adding a span to a `Date`, the length between two dates under a day-count convention (ACT/360, 30/360). This is deterministic, universal Gregorian math (month lengths, leap years), with no external dependency and no retroactive mutation, so it *belongs* in the kernel for the same reason timezones do not. The dividing test is whether a rule changes over time or by jurisdiction: the Gregorian calendar does not (the kernel owns the math), while holidays and DST do (claims own the consensus). Civil dates are leap-second-free, so the determinism is total - instant-level leap seconds, were they ever needed, would themselves be versioned data, not kernel constants. It awaits the worked example that forces it - an interest-accrual or covenant-rollover programme is the natural one - and when it lands it follows the `Mul`/`Div` line: date math in a *gate* or *read-side projection* is inside, date math that *produces a stored figure* stays in the outside-compute zone. (2) **The ergonomics, needing no kernel change:** a standard-calendar import that emits a holiday set (TARGET, SIFMA, an exchange schedule) as versioned `BusinessDay` / `Holiday` claims, plus business-day conventions ("modified following") as reusable `define`s - what makes calendars-as-claims pleasant to author rather than a hand-typed wall. The doctrine itself stays: a holiday set is *data that changes over time*, so consulting a runtime tzdb would make a date-dependent decision replay differently once the calendar updates - the same replay-determinism argument that keeps the clock out of the kernel. Real contracts already name their calendar authority (ISDA, TARGET) and its version; modelling it as a provenance-carrying claim is more faithful, not less. The runtime-lookup convenience is what an *audit* engine specifically cannot afford.
- **Port-local time as admitted claims.** The laytime example's next stage: local calendar days (the 25-hour fall-back day, "weather working days") entering as claims from an external calendar authority - `PortDay(port, local_date, starts_at, ends_at, tzdb_version)`-shaped - never as a runtime tzdb lookup. The forcing argument is replay determinism; the principle is that external interpretive knowledge enters as claims, not hidden runtime assumptions. Statement-of-Facts correction after a settled demurrage claim arrives in the same stage.
- **Unit conversions as claims.** Units themselves shipped (the laytime example's cargo tonnage and demurrage money forced `Decimal[t]` / `Decimal[USD]` - contractual labels on exact decimals, same-unit-only algebra, scaling, ratios). What remains deliberately out is everything relating one unit to another: conversion factors, FX rates, market-convention tables. Those are domain knowledge with provenance and time (whose table? as of when?), so they enter as supersedable admitted claims when an example genuinely needs one - never as kernel constants. No compound units and no unit-producing arithmetic until a worked example argues otherwise.
- **Spatial values - admission rules that depend on where something is.** Quantity, time, and authority are governed; *location* is not. A point is two opaque decimals today, with no `within` / `contains` / `intersects` the runtime can act on - so "this delivery is only legitimate inside the contracted zone" or "these two land claims must not overlap" cannot be written as admission law. The forcing example is a spatial *gate* (a delivery or policy admitted only if a location lies inside a permitted region - energy bidding zones, catastrophe-zone underwriting) or a spatial *invariant* (no two claims cover overlapping area, the spatial form of no-double-counting that extends the carbon example). It forces a point and region value kind - exact decimal coordinates, no floats in a boundary test - and an in-kernel geometric predicate, evaluated in memory like every other rule and never delegated to PostGIS: a host call during admission would break deterministic replay (the same argument that keeps the clock out of the kernel), so PostGIS, if it ever enters, is a downstream-query concern. And, like timezones and calendars, the geometry must carry or reference an explicit coordinate reference system and region authority as admitted data: the kernel may own deterministic topology (containment, overlap) over geometry it is *given*, never hidden projection, geodesy, or map-version interpretation - those change over time and by authority, so baking them in would make a boundary test replay differently as the map updates. General GIS - distance, routing, projections, spatial analytics - stays out of scope.
- **`morph fmt` (canonical formatter as a CLI).** The formatter exists in `morpholog-core::format` and is coupled to the parser by the round-trip property test. A CLI front-end (`morpholog fmt <file.morph>` or `--check` mode) lands when a project's worth of `.morph` files starts to feel like editorial drift; not before.
- **Integration / external-compute provenance.** The same primitive (claims) reaching the system's edges: external-computation results admitted as provenance claims, outbox intents acquiring delivery/acknowledgement claims, and actor authority extending to delegated and external actors. The longest-horizon direction; deliberately unspecified in detail until the embedder forces a concrete shape.

## Deliberately out of scope (revisit only with explicit reason)

The doctrinal floors - no entities/classes/services/ORM, no workflow engine, no arbitrary computation inside transformations, no BI engine, no solver runtime, no ad-hoc query DSL, no bypass flags ever - all hold here; they and their reasons live in [`scope-and-ambition.md`](scope-and-ambition.md)'s Non-goals and are not re-listed. The deferrals specific to the operational plan:

- No self-hosting. Morpholog governs business state; the compiler is Rust.
- Tree-sitter / LSP grammars: the parser is the forcing pressure. Deferred until an authoring workflow puts real demand on them.

## How to read this file

If something appears on the "awaiting a worked example" list, that's a *deliberate* hold, not a TODO. The project's central discipline is that kernel primitives arrive alongside the example that forces them; reading the list as a backlog defeats the discipline.

When something ships, it leaves this file - the README says what Morpholog does, [`design-history.md`](design-history.md) says what forced it. Moving an item from "awaiting" to "imminent" goes through the same review/PR loop as code changes. The roadmap moves; the doctrine in [`scope-and-ambition.md`](scope-and-ambition.md) does not.
