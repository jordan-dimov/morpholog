[package]
name = "morpholog-witness"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "External witnesses to Morpholog audit tree heads: RFC 3161 request building, response self-check, and offline verification against verifier-chosen anchors"

[dependencies]
cryptographic-message-syntax = { version = "0.28", default-features = false }
x509-certificate = "0.25"
bcder = "0.7"
bytes = "1"
sha1 = "0.11"
sha2 = { workspace = true }
chrono = { workspace = true }
rand = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
