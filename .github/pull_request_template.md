<!-- Closes #N, or Addresses #N when part of an issue stays open (say which part). -->

## What

<!-- The change in the shape the reader acts on. -->

## Contract changes

<!-- Which pinned surface moved, or "none": an envelope ($defs + golden + generated client, all three), a migration (its number and guard), a verifier verdict, a CLI flag. Name the test that pins each. -->

## Plan review

<!-- For anything touching a pinned envelope, a migration, or a verifier verdict: what the plan review changed before code, or why none was needed. -->

## Proof

| Claim | Command |
|---|---|
| | |

<!-- One row per claim the description makes, each with the command that proves it, and "break-checked" where a gate was broken and watched go red for its own reason. Everything CI runs: `./scripts/precommit.sh` with `DATABASE_URL`, exit 0. -->

## Not done

<!-- What was left out deliberately and why, and what would force it. -->
